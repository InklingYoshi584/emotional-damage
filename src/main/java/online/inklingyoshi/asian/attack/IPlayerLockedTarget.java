package online.inklingyoshi.asian.attack;

import java.util.UUID;

public interface IPlayerLockedTarget {
    UUID emotionalDamage$getLockedTarget();
    void emotionalDamage$setLockedTarget(UUID uuid);
}
